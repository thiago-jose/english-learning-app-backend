{
  "name": "english-learning-app-backend",
  "version": "1.0.0",
  "description": "Backend for English Learning App",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "predeploy:dev": "npm run build",
    "predeploy:prod": "npm run build",
    "watch": "tsc -w",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest --config jest.config.js --testMatch='**/__tests__/unit/**/*.test.ts'",
    "test:integ": "jest --config jest.config.js --testMatch='**/__tests__/integ/**/*.test.ts'",
    "lint": "eslint . --ext .ts",
    "sam:build": "sam build",
    "sam:build:clean": "npm run clean && sam build",
    "sam:deploy:dev": "npm run sam:build && sam deploy --config-env dev --stack-name english-learning-app-dev --no-confirm-changeset",
    "sam:deploy:prod": "npm run sam:build && sam deploy --config-env prod --stack-name english-learning-app-prod --no-confirm-changeset",
    "sam:delete:dev": "sam delete --config-env dev --stack-name english-learning-app-dev",
    "sam:delete:prod": "sam delete --config-env prod --stack-name english-learning-app-prod",
    "generate-env:dev": "ts-node scripts/generate-env.ts dev",
    "generate-env:prod": "ts-node scripts/generate-env.ts prod",
    "test:integ:dev": "npm run generate-env:dev && npm run test:integ",
    "test:integ:prod": "npm run generate-env:prod && npm run test:integ"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.0.0",
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-transcribe": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.850.0",
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1001.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.136",
    "@types/aws-sdk": "^2.7.4",
    "@types/axios": "^0.9.36",
    "@types/dotenv": "^6.1.1",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.11.24",
    "@types/uuid": "^9.0.6",
    "@typescript-eslint/eslint-plugin": "^7.1.0",
    "@typescript-eslint/parser": "^7.1.0",
    "axios": "^1.11.0",
    "dotenv": "^16.5.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
