name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  AWS_REGION: us-east-1
  NODE_VERSION: '18.x'

jobs:
  test-and-deploy-dev:
    runs-on: ubuntu-latest
    environment: development
    steps:
    - uses: actions/checkout@v3
    
    - name: Use Node.js ${{ env.NODE_VERSION }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run linter
      run: npm run lint
    
    - name: Run unit tests
      run: npm run test:unit
    
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_DEV }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_DEV }}
        aws-region: ${{ env.AWS_REGION }}
    
    - name: Build SAM application
      run: npm run sam:build
    
    - name: Deploy to development
      run: |
        npm run sam:deploy:dev -- --no-fail-on-empty-changeset
    
    - name: Generate development environment
      run: npm run generate-env:dev
    
    - name: Run integration tests
      run: npm run test:integ
      env:
        NODE_ENV: dev

  # Production deployment is disabled for now
  # build-and-deploy-prod:
  #   needs: test-and-deploy-dev
  #   if: github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   environment: production
  #   steps:
  #   - uses: actions/checkout@v3
  #   
  #   - name: Use Node.js ${{ env.NODE_VERSION }}
  #     uses: actions/setup-node@v3
  #     with:
  #       node-version: ${{ env.NODE_VERSION }}
  #       cache: 'npm'
  #   
  #   - name: Install dependencies
  #     run: npm ci
  #   
  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v1
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_PROD }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_PROD }}
  #       aws-region: ${{ env.AWS_REGION }}
  #   
  #   - name: Build SAM application
  #     run: npm run sam:build
  #   
  #   - name: Deploy to production
  #     run: |
  #       npm run sam:deploy:prod -- --no-fail-on-empty-changeset
  #   
  #   - name: Generate production environment
  #     run: npm run generate-env:prod 